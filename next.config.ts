import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configurações padrão do Next.js
  // Removido output: 'standalone' pois a Hostinger gerencia o servidor Next.js nativamente
};

export default nextConfig;
